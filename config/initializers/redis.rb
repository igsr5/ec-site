# ==============================================================================
# config - initializers - redis
# ==============================================================================
# Check Redis connection
# Redis.new.ping

# Redis.current = Redis.new(db: (Settings.redis.db || 3))
