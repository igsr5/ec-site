p = @receipt.id
p = @receipt.address_id
p = @receipt.cart_id
p = @receipt.card_id
p = @receipt.total_price_tax
p = @receipt.created_at

p = @receipt.cart.order_details.first.product.title
p = @receipt.address.postal_code
p = @receipt.card.card_num
